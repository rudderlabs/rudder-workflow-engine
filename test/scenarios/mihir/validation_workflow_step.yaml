steps:
  - name: validateOp
    template: |
      $assert(op in ["sumArrayElements", "maxElementFromAnArray", "minElementFromAnArray", "averageOfAnArray","sizeOfAnArray"], "operation is not supported")
