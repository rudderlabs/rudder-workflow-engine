steps:
  - name: isLeapYear
    description:
    condition: (year%400 = 0 or (year%4=0 and year%100!=0))
    template: |
      year & " is a leap year"
  - name: isNotALeapYear
    template: |
      year & " is not a leap year"
